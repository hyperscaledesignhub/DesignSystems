import React from 'react';
import { Typography } from '@mui/material';

function LiveTransactions({ ws }) {
  return <Typography variant="h4">Live Transaction Feed (Coming Soon)</Typography>;
}

export default LiveTransactions;